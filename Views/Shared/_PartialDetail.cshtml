@Html.ActionLink(SessionService.GetActionName(enAction.Detail), ActionService.Detail, ActionService.Controller, new
    { area = ActionService.Area, id = ActionService.RowId }, new
    { @class = "btn btn-secondary" })